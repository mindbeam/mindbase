@first = "First"
@second = Text("Second")
@go_again = @first
@look_ahead = @agent
@url = Url("https://mindbeam.dev")
@agent = Agent(default)
@data = DataNode({"Something"}; "the data is here")
$here = Symbolize(@data)